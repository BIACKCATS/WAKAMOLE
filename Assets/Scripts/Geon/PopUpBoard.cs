/*
using UnityEngine;
using DG.Tweening;
using static UnityEditor.Profiling.RawFrameDataView;

public class PopUpBoard : MonoBehaviour
{
    [SerializeField] private GameObject ClearBoard;
    private void Awake()
    {
        ClearBoard.transform.DOScale(new Vector3(0, 0, 0), 0.1f);
    }
    private void OnEnable()
    {
        ClearBoard.transform.DOScale(new Vector3(0.7f, 0.8f, 1), 1f).SetEase(Ease.OutBack).SetEase(Ease.OutBounce);
    }
}
*/
